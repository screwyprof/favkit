#![cfg_attr(coverage_nightly, feature(coverage_attribute))]

use favkit;

#[test]
fn it_works() {
    // Basic sanity check that the library can be imported
    assert!(true, "Library can be imported");
}